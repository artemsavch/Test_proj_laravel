<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Huge Stas' butt</title>
    <style>
        .text-name{
            color: red;
            font-size: 54px;
            font-weight: 100;
            position: absolute;
            top: 300px;
            left: 700px;
        }
    </style>
</head>
<body>
    <h1>
     {{--   @foreach($goods as $good)
           @foreach($good as $val)
                {{$val}}
                @if(!$loop->last)
                     -
                @endif
            @endforeach
            <br>
        @endforeach--}}
        {{$test}}      {{$test2}}   {{$test3}}
        <br>
        <br>
        {{$a}}    {{$b}}    {{$c}}    {{$d}}
        <br>
        <br>
    </h1>

    <h2 class="text-name">
        Artem Orel
    </h2>
</body>
</html>
